TYPE: policy
WHY: running every corpus file a policy change revises at its subject content against the candidate verifier instead of restoring the trusted copy over it and never running the revision, because a revised counterexample that cannot hold on the shipped verifier otherwise merges and the next unrelated policy change is the one refused.
SEARCHED: rg -n "verifier_selftest|ACCEPTANCE" scripts/tests/ → nearest: test_trust_gaps_v04.py:121-156 (added corpus files, failing and passing), test_trust_core_v04.py:43-88 (trusted corpus restored over a stub, policy control), test_adoption_prerequisites.py:84-134 (ACCEPTANCE on a test/ change) | NONE-FITS: none revises or deletes an existing scripts/tests/ file under an ACCEPTANCE line, so none observes a revision being run at its subject content or a deletion being named.
SEARCHED: git show main:scripts/gate_checks.py | rg -n "ls-tree|def show|def ls_tree|startswith\(\"test_\"\)|def changed_files" → 5 hits in scripts/gate_checks.py on main: :150 def show, :159 def ls_tree, :160 its ls-tree call, :163 def changed_files, :1391 the inline startswith("test_") filter in check_verifier_selftest | REUSED: changed_files (--no-renames, --diff-filter D and MTA, -- scripts/tests/) is the change's own name-status diff that F1 attributes revisions and deletions from; the inline filter at :1391 is replaced by _is_selftest, not duplicated | NONE-FITS: show decodes text and returns str (and optional=None), while _place_corpus must write the exact bytes of a corpus file, so it calls git(..., binary=True); ls_tree runs --name-only and returns names without blob ids, while _corpus_blobs needs the blob id per path to tell a revision that changes bytes from one that does not — the three helpers are private (F3) and add no public symbol.
READ: the plan left open where deletion visibility lands; I read it as the h1 detail (the frozen-set listing kept under the ACCEPTANCE exemption) and the verifier_selftest detail (the absent-and-restored note, on every outcome including the timeout ERROR branches); the other reading was test_inventory; the plan's extension of collect_ids to scripts/tests/ is not delivered by this change; granted-by: none.
TESTS: every command on these TESTS lines runs from the worktree root with the verifier named for it in place as scripts/gate_checks.py; the venv holds pytest, pytest-xdist and ruff.
TESTS: ruff check --isolated --line-length 110 scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → All checks passed.
TESTS: tests first, verifier as on main: python -m pytest -q -p no:cacheprovider scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → 8 failed, 7 passed in 57.08s — failing: test_a_revised_counterexample_that_cannot_hold_is_refused_s1, test_a_revised_helper_is_run_under_every_trusted_test_s1, test_a_revised_counterexample_is_named_by_h1_with_its_commit_s3, test_a_deleted_counterexample_is_named_by_h1_and_selftest_s2_s3, test_a_revision_that_removes_a_name_a_sibling_imports_is_refused_s1, test_an_emptied_revision_a_sibling_imports_from_is_refused_s1, test_a_revision_that_fails_to_import_is_fail_not_error_pin, test_a_deletion_is_named_when_the_selftest_times_out_pin_f2 (main neither runs a revision nor names a revised or deleted path); the other 7 pass on main.
TESTS: comparison, the earlier branch's full-corpus versions of the same three files against the verifier as on main, in a copy of the tree without .git (rsync -a --exclude .git ./ into an empty scratch directory, the three files replaced there): python -m pytest -q -p no:cacheprovider -n 8 scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → 8 failed, 7 passed in 122.76s (0:02:02) with -n 8 (serially about 800-1000s) — the same 8 test names fail as with the minimal-fixture files above.
TESTS: the reviewed final verifier (scripts/gate_checks.py blob e4f7419, byte-identical to the earlier branch's reviewed final state) in a copy of the tree without .git (rsync -a --exclude .git ./ into an empty scratch directory, scripts/gate_checks.py replaced there): python -m pytest -q -p no:cacheprovider scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → 15 passed in 58.23s serially (no -n; the slowest test 6.63s).
TESTS: sentinel check, in a copy of the tree without .git (rsync -a --exclude .git ./ into an empty scratch directory): python -c "import sys; sys.path.insert(0, 'scripts/tests'); import test_revised_counterexamples_r1c as m; open('scripts/tests/test_zz_sentinel_probe.py', 'w').write(m.SENTINEL)" && python -m pytest -q -p no:cacheprovider scripts/tests/test_zz_sentinel_probe.py → 1 passed on the verifier as on main and 1 passed on the final verifier; the guard fixture on the stub verifier, driven from python with the test module's own helpers, gives verifier_selftest FAIL with both test_r1c_sentinel.py and test_r1c_case.py failing in the trusted corpus.
TESTS: mutation, on the final verifier, one mutant per line below, each applied to a fresh copy of the tree without .git (rsync -a --exclude .git ./ into an empty scratch directory, commands then run from there); killed means pytest exited nonzero. Score 9/11 = 0.82, the same mutants killed and the same two survivors as with the full-corpus fixtures: no committed test gates a change that both revises a corpus file and adds one whose outcome depends on it (M10), and none adds a scripts/tests/ path named test_* that is not a .py module (M11).
TESTS: M1 skip the revised run: python -c 'import sys;p,a,b=sys.argv[1:];s=open(p).read();assert s.count(a)==1,a;open(p,"w").write(s.replace(a,b))' scripts/gate_checks.py 'if revised else []' 'if False else []' && python -m pytest -q -p no:cacheprovider -n 8 -x scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → killed (4 failed, 9 passed in 11.63s)
TESTS: M2 count a revision as holding on failure: python -c 'import sys;p,a,b=sys.argv[1:];s=open(p).read();assert s.count(a)==1,a;open(p,"w").write(s.replace(a,b))' scripts/gate_checks.py 'if res1.returncode != 0:' 'if False:' && python -m pytest -q -p no:cacheprovider -n 8 -x scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → killed (4 failed, 9 passed in 10.74s)
TESTS: M3 select only the revised files as targets: python -c 'import sys;p,a,b=sys.argv[1:];s=open(p).read();assert s.count(a)==1,a;open(p,"w").write(s.replace(a,b))' scripts/gate_checks.py 'targets = [p for p in trusted if _is_selftest(p)]' 'targets = [p for p in revised if _is_selftest(p)]' && python -m pytest -q -p no:cacheprovider -n 8 -x scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → killed (3 failed, 11 passed in 10.84s)
TESTS: M4 attribute revisions from policy-ref blobs instead of the diff: python -c 'import sys;p,a,b=sys.argv[1:];s=open(p).read();assert s.count(a)==1,a;open(p,"w").write(s.replace(a,b))' scripts/gate_checks.py 'if p in own_revised and p in subject_blobs' 'if p in subject_blobs' && python -m pytest -q -p no:cacheprovider -n 8 -x scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → killed (1 failed, 14 passed in 11.32s)
TESTS: M5 attribute deletions from policy-ref blobs instead of the diff: python -c 'import sys;p,a,b=sys.argv[1:];s=open(p).read();assert s.count(a)==1,a;open(p,"w").write(s.replace(a,b))' scripts/gate_checks.py 'restored = [p for p in trusted if p in own_deleted]' 'restored = [p for p in trusted if p not in subject_blobs]' && python -m pytest -q -p no:cacheprovider -n 8 -x scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → killed (2 failed, 13 passed in 11.12s)
TESTS: M6 drop the restored note on every branch: python -c 'import sys;p,a,b=sys.argv[1:];s=open(p).read();assert s.count(a)==1,a;open(p,"w").write(s.replace(a,b))' scripts/gate_checks.py 'if restored else [])' 'if False else [])' && python -m pytest -q -p no:cacheprovider -n 8 -x scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → killed (2 failed, 12 passed in 10.91s)
TESTS: M7 drop the restored note on the trusted-run timeout ERROR: python -c 'import sys;p,a,b=sys.argv[1:];s=open(p).read();assert s.count(a)==1,a;open(p,"w").write(s.replace(a,b))' scripts/gate_checks.py 'the trusted corpus exceeded UNIT_TESTS_TIMEOUT={timeout}s"] + restored_note' 'the trusted corpus exceeded UNIT_TESTS_TIMEOUT={timeout}s"]' && python -m pytest -q -p no:cacheprovider -n 8 -x scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → killed (1 failed, 14 passed in 11.18s)
TESTS: M8 drop the h1 frozen listing under the ACCEPTANCE exemption: python -c 'import sys;p,a,b=sys.argv[1:];s=open(p).read();assert s.count(a)==1,a;open(p,"w").write(s.replace(a,b))' scripts/gate_checks.py 'if frozen:' 'if False:' && python -m pytest -q -p no:cacheprovider -n 8 -x scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → killed (2 failed, 11 passed in 11.31s)
TESTS: M9 run the revised paths at their policy-ref content: python -c 'import sys;p,a,b=sys.argv[1:];s=open(p).read();assert s.count(a)==1,a;open(p,"w").write(s.replace(a,b))' scripts/gate_checks.py '_place_corpus(ctx, exp, ctx.subject, revised)' '_place_corpus(ctx, exp, ctx.policy_ref, revised)' && python -m pytest -q -p no:cacheprovider -n 8 -x scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → killed (4 failed, 9 passed in 10.85s)
TESTS: M10 leave the revised paths in place for the added-cases run: python -c 'import sys;p,a,b=sys.argv[1:];s=open(p).read();assert s.count(a)==1,a;open(p,"w").write(s.replace(a,b))' scripts/gate_checks.py '_place_corpus(ctx, exp, ctx.policy_ref, revised)' 'pass' && python -m pytest -q -p no:cacheprovider -n 8 -x scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → survived (15 passed in 11.13s)
TESTS: M11 pass every added scripts/tests/ path to pytest (drop the _is_selftest filter): python -c 'import sys;p,a,b=sys.argv[1:];s=open(p).read();assert s.count(a)==1,a;open(p,"w").write(s.replace(a,b))' scripts/gate_checks.py 'if _is_selftest(p))' 'if True)' && python -m pytest -q -p no:cacheprovider -n 8 -x scripts/tests/test_revised_counterexamples_r1c.py scripts/tests/test_revised_counterexamples_r1c_siblings.py scripts/tests/test_revised_counterexamples_r1c_attribution.py → survived (15 passed in 11.32s)
HANDOFF: (a) the rule this change enforces as a FAIL ("a counterexample you revise must hold against the verifier you ship") is not in enacted text; by the task owner's decision of 2026-09-25 this change merges only after a separate legislative policy/ change enacts that rule | (b) plan items R1(a) and R1(b) were not implemented and were retired by the task owner on 2026-09-25 | (c) observed on main, not changed here: masking_markers scans 0 added lines under scripts/ because DOC_EXCLUDE exempts scripts/gate_checks.py and scripts/tests/.
HANDOFF: non-reproducible review probes (scratch files outside the repository, not kept; recorded as what was observed, not as evidence a reviewer can replay): round 1, in a git-archive copy of the tests-first commit, scratch scripts/tests/test_r1c_case.py and scripts/tests/test_r1c_rev.py → the case on main passes and its revision fails on the shipped verifier (1 failed, 1 passed); with _harness.py's outcome() revised to .lower(), test_r1c_case.py → 1 failed; a candidate-verifier revision asserting '# r1c-candidate-marker' in the nested repo's scripts/gate_checks.py → 1 failed on the unchanged verifier, 1 passed after appending the marker. Round 2, KIT pointed at the worktree, scratch r1c-review/scen_test.py -k 'test_a_ or test_i_ or test_j_' → [a] verifier_selftest=FAIL (was ERROR), [i] PASS and merge_eligible (was ERROR), [j] ERROR naming scripts/tests/test_gone.py as absent at the subject and restored.
HANDOFF: where to look first if this breaks: check_verifier_selftest in scripts/gate_checks.py — own_deleted and own_revised come from changed_files(ctx, ...) over base..subject, so a wrong BASE (not the fork point) misattributes exactly as the policy-ref comparison did; the whole-tree make gate has not been run on this branch by the implementer.
HANDOFF: known residuals, not fixed here: (d) the added-files set still compares the policy ref with the subject, so on a branch that forked before main deleted a corpus test, that test is run and reported as added by this change (same attribution class as F1, outside this spec); (e) mutation survivors M10 and M11 are unpinned; (f) the section 7 audit of this policy change by a fresh instance has not been run.
HANDOFF: this change was first developed on an earlier unpublished branch whose three test files built every fixture with the kit's whole self-test corpus (test_gate_checks.py, 27 nested gates, re-run for each revision), so they took about 800-1000s serially and exceeded UNIT_TESTS_TIMEOUT (900s) in the gate's added-files arm (verifier_selftest ERROR on timeout); the tests were rewritten on minimal fixture corpora (scripts/tests/_harness.py plus the small counterexamples each scenario needs, and for the stub guard one sentinel counterexample, revised in place of test_gate_checks.py) with the same test names, scenarios and assertions; the verifier this change ships is byte-identical to that branch's reviewed final state (scripts/gate_checks.py blob e4f7419), and its TESTS history is not repeated here.
