TYPE: policy
WHY: Repo.commit unstages the paths the harness itself wrote (the .verifiers/ copy and the gate's --out file) instead of each test hiding them with .git/info/exclude, because the trap lives in one harness method and every test that commits after a gate run inherits it.
SEARCHED: rg -n "add\", \"-A|def commit|\.verifiers|\.gate" scripts/tests/ → 95 hits in 15 files; 86 are `r.gate(` call sites (85 in test files, 1 in the harness docstring; callers, not helpers); the rest: scripts/tests/_harness.py:88-89 (Repo.commit, `git add -A`), _harness.py:109 (Repo.verifier_from writes one .verifiers/ file per ref), _harness.py:121 (Repo.gate, out=".gate/result.json"), test_gate_checks.py:37-38 (its own commit_all with `git add -A`), test_gate_checks.py:68 (its own run_gate writing .gate/result.json), test_plumbing_v04.py:90 (reads .gate/result.json after a gate run), test_trust_core_v04.py:162 (its own --out under .gate/) | REUSED: scripts/tests/_harness.py Repo.commit, Repo.verifier_from, Repo.gate, Repo.write — the fix is inside them, no new helper module | NONE-FITS: test_gate_checks.py:37-38 and :68 are a separate fixture private to that frozen file, not a helper to reuse; test_plumbing_v04.py:90 and test_trust_core_v04.py:162 are readers of the output location, which this change leaves where it is.
ACCEPTANCE: approver: repository owner Ju1se, who requested this edit on 2026-09-25; reason: Repo.commit stages the harness's own run outputs (.verifiers/, the gate result) into the change under judgment; old: commit stages every file in the work tree; new: commit leaves paths the harness itself wrote unstaged unless the test wrote that path itself; evidence: the per-test outcomes of the whole corpus before and after and a before/after probe, in TESTS.
TESTS: Probe (a throwaway script outside the repository: Repo → legal_feature → gate("feature/greeting") → write docs/notes.md → commit → git show --name-only HEAD), run once with scripts/tests/_harness.py from main and once with this change → before: .gate/result.json, .verifiers/main.py, docs/notes.md; after: docs/notes.md. Second probe, a gate out= that is a glob pattern matching a path the test wrote itself (write gate/res.json → gate(out="g[a]te/res.json") → write docs/x.md → commit), which is why the unstaging runs `git --literal-pathspecs reset`: before: .verifiers/main.py, docs/x.md, g[a]te/res.json, gate/res.json; after: docs/x.md, gate/res.json (the test's own file committed, the harness's gate output left untracked). Whole corpus, on a scratch copy of this tree with scripts/tests/_harness.py restored from main (every other file identical to main): python -m pytest scripts/tests -n 8 -rA -q -p no:cacheprovider → 147 passed; the same command on this tree → 147 passed; per-node-id outcomes compared (PASSED/FAILED/ERROR) → 0 flips. Compatibility, a scratch copy of this tree plus the open PRs' scripts/tests/test_revised_counterexamples_r1c.py, test_revised_counterexamples_r1c_attribution.py, test_revised_counterexamples_r1c_siblings.py and scripts/gate_checks.py (policy/counterexamples-hold-law) and scripts/tests/test_balance_controls.py (balance controls): python -m pytest scripts/tests -n 8 -rA -q -p no:cacheprovider → 166 passed. ruff check --isolated --select E722,BLE001,S110,S112,S113 scripts/tests/_harness.py → clean.

HANDOFF: a counterexample pinning the new commit behaviour (commit after a gate run carries no .verifiers/ or .gate/ path; a path the test wrote itself under those directories is still committed) cannot land in this change, because verifier_selftest restores main's _harness.py before it runs added tests and would run it against the unfixed harness — add it as a policy/ change after this merges. Once merged, the .git/info/exclude workarounds the two open PRs carry for this trap can be dropped (in the balance-controls PR, scripts/tests/test_balance_controls.py writes `.verifiers/` and `.gate/` into .git/info/exclude; the counterexamples-hold-law PR's test files as copied for the compatibility run contain no such line, so check its branch for one before closing this out). Not covered here: scripts/tests/test_gate_checks.py has its own commit_all with `git add -A` and its own run_gate writing .gate/result.json, a separate fixture in a frozen file; look there first if a leftover .gate/ path shows up in a commit made by that file's tests.
